version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Vibely-frontend
    ports:
      - "4200:80"   # access at http://localhost:4200
    restart: unless-stopped